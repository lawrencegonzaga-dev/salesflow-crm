

========== src/styles/base.css ==========

body {
    font-family: Arial, sans-serif;
    font-size: 16px;
    line-height: 1.5;
}

h1, h2, h3, h4, h5, h6, p{
    margin: 0;
}


.page-title {
    font-size: 24px;
    line-height: 32px;
    font-weight: 600;
}

.body-text {
    font-size: 14px;
    line-height: 20px;
}

.caption {
    font-size: 12px;
    line-height: 16px;
}
/* base.css */


========== src/styles/crm.css ==========

body { margin: 0; background: var(--color-page); color: var(--color-text); font-family: var(--font-family); }
#root { min-height: 100vh; }
.app-header { background: var(--color-surface); border-bottom: 1px solid var(--color-border); color: var(--color-text); }
.app-main { background: var(--color-page); }
.app-sidebar h2 { margin: 0 0 28px; font-size: 22px; letter-spacing: -.5px; }
.page-title { margin: 0; }
.body-text { color: var(--color-text-muted); margin-top: 4px; }
.card, .stat-card, .kanban-column { background: var(--color-surface); border: 1px solid var(--color-border); border-radius: var(--radius-md); box-shadow: var(--shadow-sm); }
.card { padding: var(--card-padding); }
.card-title { font-size: var(--text-lg); margin: 0 0 16px; }
.record-form, .contacts-content form, .leads-content form { display: grid; grid-template-columns: repeat(auto-fit, minmax(180px, 1fr)); gap: 16px; align-items: end; }
.form-group { min-width: 0; }
.form-label { color: var(--color-text); }
input, select { width: 100%; height: var(--input-height); padding: 0 10px; color: var(--color-text); background: var(--color-surface); border: 1px solid var(--color-border); border-radius: var(--radius-sm); }
.form-actions { display: flex; align-items: end; gap: 8px; }
button { min-height: 32px; padding: 0 12px; color: var(--color-text); background: transparent; border: 1px solid var(--color-border); border-radius: var(--radius-sm); }
button:hover { border-color: var(--color-action); color: var(--color-action); }
.button { height: 40px; }
.button--primary { color: white; background: var(--color-action); border-color: var(--color-action); }
.button--danger { color: var(--color-danger); }
.toolbar { display: flex; flex-wrap: wrap; gap: 12px; }
.toolbar input { flex: 1 1 260px; }.toolbar select { flex: 0 1 190px; }
.table-wrap { overflow-x: auto; background: var(--color-surface); border: 1px solid var(--color-border); border-radius: var(--radius-md); }
table, .table { width: 100%; border-collapse: collapse; text-align: left; background: var(--color-surface); }
th, td { padding: 13px 16px; border-bottom: 1px solid var(--color-border); white-space: nowrap; }
th { color: var(--color-text-muted); font-size: var(--text-xs); text-transform: uppercase; letter-spacing: .04em; }
tr:last-child td { border-bottom: 0; }.row-actions { display: flex; gap: 6px; }
.badge { display: inline-flex; padding: 3px 8px; font-size: var(--text-xs); color: var(--color-action); background: color-mix(in srgb, var(--color-action) 10%, transparent); border-radius: var(--radius-full); }.badge--high { color: var(--color-danger); }.badge--medium { color: var(--color-warning); }.badge--low { color: var(--color-success); }
.kanban-board { display: grid; grid-template-columns: repeat(6, minmax(210px, 1fr)); gap: 16px; min-width: 1360px; }.deals-content { overflow-x: auto; }
.kanban-column { padding: 16px; background: var(--color-surface-muted); }.kanban-column_header { min-height: auto; margin-bottom: 12px; }.kanban-column_header h2 { font-size: var(--text-base); }
.deal-card { display: grid; gap: 7px; padding: 14px; margin-bottom: 10px; background: var(--color-surface); border: 1px solid var(--color-border); border-radius: var(--radius-sm); }.deal-card h3, .deal-card p { margin: 0; }.deal-card p, .deal-card small { color: var(--color-text-muted); }.deal-card strong { color: var(--color-action); }
.kanban-empty { margin: 8px 0; color: var(--color-text-muted); font-size: var(--text-sm); text-align: center; }
.task-description, .task-date-group { display: block; margin-top: 3px; color: var(--color-text-muted); font-size: var(--text-xs); white-space: normal; }
.task-due--overdue { color: var(--color-danger); font-weight: 600; }
.task-status { font-size: var(--text-sm); }
.task-empty { padding: 32px; color: var(--color-text-muted); text-align: center; }
.dashboard-stat { grid-template-columns: repeat(4, minmax(0, 1fr)); }.stat-card { display: grid; gap: 6px; padding: 20px; }.stat-card span, .stat-card small { color: var(--color-text-muted); }.stat-card strong { font-size: 26px; }.dashboard-bottom { grid-template-columns: minmax(0, 1.4fr) minmax(320px, 1fr); }
.dashboard-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: var(--section-gap); }
.summary-list { display: grid; gap: 9px; }.summary-list__row { display: flex; justify-content: space-between; padding-bottom: 8px; border-bottom: 1px solid var(--color-border); }.summary-list__row:last-child { padding-bottom: 0; border-bottom: 0; }
.dashboard-task-groups { display: grid; grid-template-columns: repeat(3, 1fr); gap: 12px; }.dashboard-task-group { display: grid; gap: 4px; padding: 16px; border-radius: var(--radius-sm); background: var(--color-surface-muted); }.dashboard-task-group strong { font-size: 26px; }.dashboard-task-group span { color: var(--color-text-muted); font-size: var(--text-sm); }.dashboard-task--overdue strong { color: var(--color-danger); }.dashboard-task--today strong { color: var(--color-warning); }.dashboard-task--upcoming strong { color: var(--color-action); }
.report-content { display: grid; gap: var(--section-gap); }.report-filters { display: grid; grid-template-columns: repeat(3, minmax(160px, 1fr)); gap: 16px; }.report-filters label { display: grid; gap: 6px; font-size: var(--text-sm); color: var(--color-text-muted); }.report-filters + .body-text { font-size: var(--text-xs); }.report-summary { display: grid; grid-template-columns: repeat(4, minmax(0, 1fr)); gap: 16px; }.report-section { margin: 0; }.report-section > .card-title { margin-bottom: 16px; }.report-metrics { display: grid; grid-template-columns: repeat(3, 1fr); gap: 12px; margin-bottom: 20px; }.report-metrics > div { display: grid; gap: 4px; padding: 14px; background: var(--color-surface-muted); border-radius: var(--radius-sm); }.report-metrics strong { font-size: 24px; }.report-metrics span { color: var(--color-text-muted); font-size: var(--text-sm); }.report-metrics__danger strong { color: var(--color-danger); }.report-rows { display: grid; gap: 10px; }.report-row { display: grid; grid-template-columns: 100px 1fr 24px; gap: 10px; align-items: center; }.report-bar { height: 8px; overflow: hidden; border-radius: var(--radius-full); background: var(--color-surface-muted); }.report-bar i { display: block; height: 100%; background: var(--color-action); border-radius: inherit; }.report-deals-table { display: grid; }.report-deals-table__header, .report-deals-table__row { display: grid; grid-template-columns: 1fr 90px 140px; gap: 12px; padding: 10px 0; border-bottom: 1px solid var(--color-border); }.report-deals-table__header { color: var(--color-text-muted); font-size: var(--text-xs); text-transform: uppercase; }.report-deals-table__row:last-child { border-bottom: 0; }
.settings-content { display: grid; gap: var(--section-gap); }.settings-form { display: grid; gap: var(--section-gap); }.settings-toggle { display: flex; align-items: center; gap: 8px; min-height: var(--input-height); }.settings-toggle input { width: auto; }.settings-save { display: flex; align-items: center; gap: 12px; }.settings-save span { color: var(--color-success); font-size: var(--text-sm); }.settings-danger { display: grid; gap: 12px; }.settings-danger .card-title, .settings-danger .body-text { margin: 0; }.settings-danger button { justify-self: start; }
.pipeline-bars { display: grid; gap: 16px; }.pipeline-row { display: grid; grid-template-columns: 75px 1fr 24px; gap: 10px; align-items: center; }.pipeline-track { height: 9px; overflow: hidden; background: var(--color-surface-muted); border-radius: var(--radius-full); }.pipeline-fill { height: 100%; background: var(--color-action); border-radius: inherit; }.activity-list { display: grid; gap: 12px; padding: 0; list-style: none; }.activity-list li { display: flex; justify-content: space-between; gap: 16px; padding-bottom: 12px; border-bottom: 1px solid var(--color-border); }.activity-list li:last-child { border: 0; padding: 0; }.activity-list div { display: grid; gap: 2px; }.activity-list span, .activity-list time { color: var(--color-text-muted); font-size: var(--text-sm); }
@media (max-width: 900px) { .dashboard-stat, .report-summary { grid-template-columns: repeat(2, 1fr); }.dashboard-bottom, .dashboard-grid { grid-template-columns: 1fr; } }
@media (max-width: 768px) { .app-header { padding: 0 16px; }.app-header_right { display: none; }.menu-button { display: inline-grid; width: 40px; place-items: center; padding: 0; font-size: 20px; }.app-main { padding: 16px; overflow-x: hidden; }.page-header { align-items: flex-start; flex-direction: column; }.card { padding: 16px; }.toolbar > div, .toolbar input, .toolbar select { flex: 1 1 100%; }.record-form, .contacts-content form, .leads-content form, .report-filters { grid-template-columns: 1fr; }.dashboard-stat, .report-summary { grid-template-columns: 1fr; }.dashboard-task-groups { grid-template-columns: 1fr; }.form-actions { align-items: stretch; }.form-actions button { flex: 1; }.row-actions { flex-wrap: wrap; }.calendar-content { width: 100%; overflow-x: auto; }.calendar-toolbar__actions { width: 100%; }.calendar-month { flex: 1; }.report-deals-table { overflow-x: auto; }.report-deals-table__header, .report-deals-table__row { min-width: 360px; } }
/* crm.css */


========== src/styles/layout.css ==========

.app-layout {
    min-height: 100vh;

    display: grid;
    grid-template-columns: var(--sidebar-width) 1fr;
    grid-template-rows: var(--header-height) 1fr;
}

.app-sidebar {
    grid-column: 1;
    grid-row: 1 / -1;
}

.app-header {
    grid-column: 2;
    grid-row: 1;

    height: var(--header-height);
    display: flex;
    align-items: center;
}

.app-main {
    grid-column: 2;
    grid-row: 2;

    padding: var(--space-8);
}

.app-main_content{
    width: min(100%, var(--content-max-width));
    margin-inline: auto;
}


/*-----------SECTION-------------------*/

.page {
    display: flex;
    flex-direction: column;

    gap: var(--section-gap);
}

.page-header {
    min-height: 56px;

    display: flex;
    align-items: center;
    justify-content: space-between;

    gap: 24px;
}

/*----------Page-----------
1. Dasahboard
2. Contact
------------------------*/

.dashboard-content {
    display: flex;
    flex-direction: column;
    gap: var(--section-gap);
    }

.dashboard-stat {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--space-6);
}

.dashboard-revenue {
    width: 100%;
}

.dashboard-bottom {
    display: grid;
    gap: var(--space-6);
}

.contacts-content {
    width: 100%;
}

.leads-content {
    width: 100%;
}

.deals-content {
    width: 100%;
    overflow-x: auto;
}

.deals-board {
    display: grid;
    grid-template-columns: repeat(4, minmax(240px, 1fr));

}

.task-content {
    width: 100%;
}

.calendar-content {
    width: 100%;
    min-height: 600px;
}

.report-content {
    display: flex;
    flex-direction: column;
    gap: var(--section-gap);
}

.report-summary {
    display: grid;
    grid-template-columns: repeat(3, 1fr);

    gap: var(--space-6);
}

.report-charts {
    display: grid;
    grid-template-columns: 2fr 1fr;

    gap: var(--space-6);
}

.settings-content {
    display: grid;
    grid-template-columns: 200px 1fr;

    gap: 32px;
}

.settings-panel {
    min-width: 0;
}


@media (max-width: 1024px) {
    .app-layout {
        grid-template-columns: 176px 1fr;
    }

    .app-sidebar {
        width: 176px;
    }
}

@media (max-width: 768px) {
    .app-layout {
        display: block;
    }

    .app-sidebar {
        position: fixed;
        z-index: 30;
        inset: 0 auto 0 0;
        width: min(280px, 82vw);
        height: 100dvh;
        transform: translateX(-105%);
        transition: transform 180ms ease;
        box-shadow: var(--shadow-lg);
    }

    .app-sidebar--open {
        transform: translateX(0);
    }

    .app-header {
        height: 56px;
    }

    .app-main {
        padding: 16px;
    }

    .navigation-backdrop {
        position: fixed;
        z-index: 20;
        inset: 0;
        width: 100%;
        height: 100%;
        padding: 0;
        background: rgb(15 23 42 / 45%);
        border: 0;
        border-radius: 0;
    }
}






/* layout.css */


========== src/styles/main.css ==========

    @import "./reset.css";
    @import "./variables.css";
    @import "./base.css";
    @import "./layout.css";
    @import "./utilities.css";

    @import "./components/sidebar.css";
    @import "./components/header.css";
    @import "./components/toolbar.css";
    @import "./components/button.css";
    @import "./components/form.css";
    @import "./components/card.css";
    @import "./components/badge.css";
    @import "./components/table.css";
    @import "./components/modal.css";
    @import "./components/kanban.css";
    @import "./components/calendar.css";
    @import "./components/leads.css";
    @import "./crm.css";

/* main.css */


========== src/styles/reset.css ==========

*,
*::before,
*::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

img {
  display: block;
  max-width: 100%;
}

button,
input,
textarea,
select {
  font: inherit;
}

button {
  cursor: pointer;
}

a {
  color: inherit;
  text-decoration: none;
}
/* reset.css */


========== src/styles/utilities.css ==========

.container {
  width: min(100%, var(--content-max-width));
  margin-inline: auto;
}

.visually-hidden {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border: 0;
}
/* utilities.css */


========== src/styles/variables.css ==========

:root {
  /* ================================
     COLORS
     ================================ */

  --color-page: #f8fafc;
  --color-surface: #ffffff;
  --color-surface-muted: #f1f5f9;

  --color-text: #0f172a; 
  --color-text-muted: #64748b;

  --color-border: #e2e8f0;

  --color-action: #2563eb;
  --color-action-hover: #1d4ed8;

  --color-success: #16a34a;
  --color-warning: #f59e0b;
  --color-danger: #dc2626;




  /* ================================
     SPACING
     ================================ */

  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-5: 20px;
  --space-6: 24px;
  --space-8: 32px;
  --space-10: 40px;
  --space-12: 48px;
  --space-16: 64px;
  
  /* ================================
     TYPOGRAPHY
     ================================ */

  --font-family: Inter, system-ui, sans-serif;

  --text-xs: 12px;
  --text-sm: 14px;
  --text-base: 16px;
  --text-lg: 18px;
  --text-xl: 20px;
  --text-2xl: 24px;
  --text-3xl: 30px;
  --text-4xl: 36px;
  
  /* ================================
     LAYOUT
     ================================ */

  --sidebar-width: 240px;
  --header-height: 64px;
  --content-max-width: 1400px;
  --page-padding: 32px;
  --page-header-height: 56px;
  --page-header-margin: 24px;
   --section-gap: 24px;
  
  /* ================================
     COMPONENTS
     ================================ */

  --input-height: 40px;
  --button-height: 40px;

  --card-padding: 24px;
  

  --radius-sm: 6px;
  --radius-md: 8px;
  --radius-lg: 12px;
  --radius-full: 999px;


  /* ================================
     SHADOWS
     ================================ */

    --shadow-sm: 0 1px 2px rgb(0 0 0 / 0.05);
    --shadow-md: 0 4px 8px rgb(0 0 0 / 0.08);
    --shadow-lg: 0 10px 20px rgb(0 0 0 / 0.10);
  
   
   
  /* ================================
     COMPONENTS
     ================================ */

     --sidebar-width: 240px;
     --sidebar-padding: 16px;
     --sidebar-logo-height: 40px;
     --nav-item-height: 40px;
     --nav-item-padding: 8px 12px;
     --nav-icon-size: 18px;
     --nav-item-gap: 12px;


     --header-height: 64px;
     --header-padding: 16px 24px;
     --header-item-gap: 16px;
     --header-icon-size: 20px;


     --page-header-min-height: 56px;
     --page-header-gap: 24px;
}

[data-theme="dark"] {
    --color-page: #0f172a;
    --color-surface: #1e293b;
    --color-border: #334155;

    --color-text: #f8fafc;
    --color-text-muted: #94a3b8;

    --color-action: #3b82f6;
    --color-action-hover: #60a5fa;

    --color-success: #22c55e;
    --color-warning: #f59e0b;
    --color-danger: #ef4444;
}
/* variables.css */


========== src/styles/components/badge.css ==========

.badge {
    display: inline-flex;
    align-items: center;

    min-height: 24px;

    padding-inline: 8px;
    font-size: 12px;
    font-weight: 500;

    border-radius: 9999%;
}

.badge--success {
    /* green success styling */
}

.badge--warning {
    /* yellow warning styling */
}

.badge--danger {
    /* red danger styling */
}

.badge--neutral {
    /* neutral styling 
Lead        → neutral
Prospect    → warning
Customer    → success
Lost        → danger
Won         → success
    
    */
}
/* badge.css */


========== src/styles/components/button.css ==========

.button {
    height: 48px;
    padding-inline: 48px;
    font-size: 14px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
}  

.button--sm {
    height: 32px;
    padding-inline: 12px;
    font-size: 13px;

}

.buton--lg {
    height: 48px;
    padding-inline: 20px;
    font-size: 20px;
}
/* button.css */


========== src/styles/components/calendar.css ==========

.calendar {
    width: 100%;
    min-width: 700px;
    border: 1px solid #e5e7eb;
    border-radius: 8px;
    overflow: hidden;
}

.calendar_header {
    min-height: 48px;
    display: grid;
    grid-template-columns: repeat(7, 1fr);

    border: 1px solid #e5e7eb;
}

.calendar_day-name {
    min-height: 48px;

    display: flex;
    align-items: center;
    justify-content: center;

    padding-inline: 8px;

    font-size: 12px;
    font-weight: 600;
}

.calendar_day-name {
    min-height: 48px;
    display: flex;
    align-items: center;
    justify-content: center;

    padding-inline: 8px;
    font-size: 12px;
    font-weight: 600;
}

.calendar_grid {
    display: grid;
    grid-template-columns: repeat(7, 1fr);

}

.calendar_day {
    min-height: 100px;
    padding: 8px;

    border-right: 1px solid #e5e7eb;
    border-left: 1px solid #e5e7eb;
}

.calendar_date {
    font-size: 14px;
}

.calendar-toolbar {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 16px;
}

.calendar-toolbar__actions { display: flex; flex-wrap: wrap; align-items: center; gap: 8px; }
.calendar-month { min-width: 150px; text-align: center; }
.calendar_day { border-bottom: 1px solid #e5e7eb; }
.calendar_day--outside { background: #f8fafc; color: #9ca3af; }
.calendar_day--today .calendar_date { display: inline-grid; width: 24px; height: 24px; place-items: center; color: #fff; background: var(--color-action); border-radius: 50%; }
.calendar_day-events { display: grid; gap: 4px; margin-top: 7px; }
.calendar-event { display: flex; overflow: hidden; align-items: center; border-radius: 4px; font-size: 11px; }
.calendar-event__title { flex: 1; min-width: 0; height: auto; padding: 3px 5px; overflow: hidden; text-align: left; text-overflow: ellipsis; white-space: nowrap; border: 0; border-radius: 0; }
.calendar-event__delete { min-height: 20px; padding: 0 4px; border: 0; border-radius: 0; }
.calendar-event--task { background: #dbeafe; }.calendar-event--deal { background: #dcfce7; }.calendar-event--event { background: #f3e8ff; }

@media (max-width: 768px) { .calendar-toolbar { align-items: flex-start; flex-direction: column; } .calendar { overflow-x: auto; } }

/* calendar.css */


========== src/styles/components/card.css ==========

.card {
    padding: 24px;
    border: 1px solid #e5e7eb;
    border: 8px;
    background-color: #ffffff;
}

.card_header {
    display: flex;
    align-items: center;
    justify-content: space-between;

    gap: 16px;
    margin-bottom: 16px;
}

.card_body {
    width: 100%;
}
/* card.css */


========== src/styles/components/form.css ==========

.form-input {
    width: 100%;
    height: 40px;

    padding-inline: 12px;

    font-size: 14px;
    border: 1px solid #d1d5db;
    border-radius: 6px;
}

.form-label {
    display: block;
    margin-bottom: 8px;
    font-size: 14px;
    font-weight: 500;
}

.form-group {
    display: flex;
    flex-direction: column;
    gap: 8px;
}
/* form.css */


========== src/styles/components/header.css ==========

.app-header {
    height: var(--header-height);

    display: flex;
    align-items: center;
    justify-content: space-between;

    padding: var(--header-padding);
}

.app-header_left, .app-header_right {
    display: flex;
    align-items: center;
    gap: var(--header-item-gap);
}

.menu-button { display: none; }
/* header.css */


========== src/styles/components/kanban.css ==========

.kanban-board {
    display: grid;
    grid-template-columns: repeat(4, minmax(240px, 1fr));

    gap: 16px;
    min-width: 1000px
    
}

.kanban-column {
    min-width: 240px;
    padding: 16px;
    border-radius: 8px;
}

.kanban-column_header {
    min-height: 80px;

    display: flex;
    align-items: center;
    justify-content: space-between;

    gap: 8px;
    margin-bottom: 16px;
}

.kanban-column_body {
    display: flex;
    flex-direction: column;

    gap: 8px;
}
/* kanban.css */


========== src/styles/components/leads.css ==========

.lead-pipeline { display: grid; grid-template-columns: repeat(6, minmax(190px, 1fr)); gap: 12px; overflow-x: auto; padding: 4px 0 16px; }
.lead-pipeline__stage { min-width: 190px; background: #f8fafc; border: 1px solid #e5e7eb; border-radius: 8px; padding: 12px; }
.lead-pipeline__stage header { display: flex; align-items: center; justify-content: space-between; }
.lead-pipeline__stage h2 { font-size: 15px; margin: 0; }
.lead-pipeline__stage header span { border-radius: 999px; background: #e5e7eb; padding: 2px 8px; font-size: 12px; }
.lead-pipeline__items { display: grid; gap: 8px; margin-top: 12px; }
.lead-pipeline__card { display: block; width: 100%; text-align: left; padding: 12px; background: #fff; border: 1px solid #e5e7eb; border-radius: 6px; cursor: pointer; color: inherit; }
.lead-pipeline__card:hover, .lead-pipeline__card:focus-visible { border-color: #2563eb; outline: none; }
.lead-pipeline__card h3 { margin: 0 0 4px; font-size: 14px; }
.lead-pipeline__card p { font-size: 12px; margin: 2px 0; }
.lead-table-wrap { overflow-x: auto; }
.lead-table__empty { padding: 32px; text-align: center; }
.lead-stage { display: inline-block; border-radius: 999px; background: #e0e7ff; padding: 2px 8px; font-size: 12px; }
/* leads.css */


========== src/styles/components/modal.css ==========

.modal {
    width: min(100% - 32px, 480px);

    padding: 24px;
    border-radius: 8px;
}

.modal_header {
    display: flex;
    align-items: center;
    justify-content: space-between;

    gap: 16px;
    margin-top: 24px;
}

/* modal.css */


========== src/styles/components/sidebar.css ==========

.app-sidebar {
    width: var(--sidebar-width);
    height: 100vh;

    padding: var(--sidebar-padding);

    display: flex;
    flex-direction: column;

    background: var(--color-surface);
    border-right: 1px solid var(--color-border);
    color: var(--color-text);
}

.sidebar-nav {
    display: flex;
    flex-direction: column;
    gap: 4px;
}

.sidebar-nav_item {
    min-height: var(--nav-item-height);

    display: flex;
    align-items: center;

    padding: var(--nav-item-padding);
    gap: var(--nav-item-gap);

    color: var(--color-text-muted);
    text-decoration: none;
    border-radius: var(--radius-sm);
}

.sidebar-nav_item.active {
    background: var(--color-action);
    color: white;
}
/* sidebar.css */


========== src/styles/components/table.css ==========

.table {
    width: 100%;
    border-collapse: collapse;
}

.table th, .table td{
height: 48px;
padding-inline: 16px;
text-align: left;   
}

.table th {
    font-size: 12px;
    font-weight: 600;
}

.table td{
    font-size:14px;
}
/* table.css */


========== src/styles/components/toolbar.css ==========

.page-toolbar {
    min-height: 40px;

    display: flex;
    align-items: center;
    justify-content: space-between;

    gap: 16px;
}

.page-toolbar_left, .page-toolbar_right {
    display: flex;
    align-items: center;
    gap: 8px;
}
/* toolbar.css */
